{
  "name": "@crypto-arena-survivors/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "EVM smart contracts (Hardhat) for Crypto Arena Survivors: RewardPool, CosmeticNFT (ERC-1155, cosmetic-only), and TournamentEscrow.",
  "license": "MIT",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.ts --network localhost",
    "clean": "hardhat clean && rm -rf deployments typechain-types"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "@openzeppelin/contracts": "^5.6.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "^22.10.5",
    "chai": "^4.5.0",
    "ethers": "^6.14.0",
    "hardhat": "^2.28.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
